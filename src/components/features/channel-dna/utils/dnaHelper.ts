/**
 * @deprecated 이 파일의 로직은 src/lib/engines/channelDnaHelper.ts 로 이전되었습니다.
 * 하위 호환을 위해 re-export만 유지합니다.
 */
export { humanizeSignal, getDnaScoreInterpretation, getSectionScoreHint } from "@/lib/engines/channelDnaHelper"
